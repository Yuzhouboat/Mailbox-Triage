# macOS
.DS_Store

# Python virtual environment
.venv/
venv/

# Python bytecode / caches
__pycache__/
*.py[cod]
*.pyo

# Local, per-checkout config — no example/template file, create these directly
skills/mailbox-triage/config/mailbox-config.toml
mailbox-config.toml

# Local, per-checkout triage rules (keep the bundled reference copy tracked)
skills/mailbox-triage/config/triage-rules.md

# Editor / OS noise
*.swp
.idea/
.vscode/

# Claude Code local (personal) settings — keep shared settings.json tracked
.claude/settings.local.json
